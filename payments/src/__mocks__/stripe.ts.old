
  /**
   * This stripe mock file is kept for reference relating to commented
   out tests in new.ts. This was a way to mock the charges call w.o.
   having to call the real stripe api.
   */

// // Implement mock stripe api client so real stripe doesn't need to be running
// export const stripe = {
//   charges: {
//     // Return promise that auto resolves as stripes charges.create method
//     create: jest.fn().mockResolvedValue({}),
//   },
// };
